import assert from "node:assert/strict";
import test from "node:test";
async function render(){const workerUrl=new URL("../dist/server/index.js",import.meta.url);workerUrl.searchParams.set("test",`${process.pid}-${Date.now()}`);const{default:worker}=await import(workerUrl.href);return worker.fetch(new Request("http://localhost/",{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("renders the audiovisual editorial workflow",async()=>{const response=await render();assert.equal(response.status,200);const html=await response.text();assert.match(html,/Do bloco ao rascunho/);assert.match(html,/Enviar arquivo/);assert.match(html,/Usar link público/);assert.match(html,/MP3, MPEG/);assert.match(html,/Por que este tema é relevante/);assert.match(html,/Qual é a principal mensagem/);assert.match(html,/Gerar página/);assert.match(html,/DEMONSTRAÇÃO PÚBLICA/);assert.doesNotMatch(html,/Enviar transcrição/);assert.doesNotMatch(html,/codex-preview|SkeletonPreview/)});
